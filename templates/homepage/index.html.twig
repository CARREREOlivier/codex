{% extends 'base.html.twig' %}

{% block title %}Accueil | Le Codex d'Uuki{% endblock %}

{% block body %}
    <div class="min-h-screen bg-parchment text-ink font-serif px-4 sm:px-6 lg:px-8 py-12">
        <div class="max-w-7xl mx-auto">
            <header class="mb-16 text-center">
                <h1 class="text-5xl font-bold mb-2">📚 Le Codex d'Uuki</h1>
                <p class="italic text-lg">Nul ne sait vraiment qui est Uuki. Certains disent qu’il écrit depuis une tour oubliée.<br>D’autres, qu’il voyage entre les mondes à travers l’encre et les pages.<br> Ce qui est certain : ses mots laissent des traces.</p>
            </header>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
                {% for article in articles %}
                    <div class="bg-white border border-[#e0dcd3] rounded-xl shadow-md hover:shadow-lg transition duration-300 p-6">
                        <h2 class="text-2xl font-bold text-accent mb-2">
                            <a href="{{ path('article_show', { slug: article.slug }) }}" class="hover:underline">
                                {{ article.title }}
                            </a>
                        </h2>
                        <p class="text-sm text-gray-500 mb-3">Publié le {{ article.createdAt|date('d M Y') }}</p>
                        <p class="text-base leading-relaxed first-letter:text-5xl first-letter:font-bold first-letter:float-left first-letter:mr-2 first-letter:leading-none first-letter:text-accent">
                            {{ article.content|striptags|slice(0, 200)|replace({'&nbsp;': ' ', '&eacute;': 'é', '&agrave;': 'à', '&quot;': '"', '&rsquo;': '’'}) ~ '…' }}
                        </p>
                    </div>
                {% else %}
                    <p class="col-span-3 text-center">Aucun article pour l’instant…</p>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
