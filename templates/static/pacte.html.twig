{% extends 'base.html.twig' %}

{% block title %}Le Pacte du Codex{% endblock %}

{% block body %}
    <div class="max-w-3xl mx-auto px-4 py-12 text-ink font-serif">
        <h1 class="text-5xl font-bold text-center mb-8">📜 Le Pacte du Codex d’Uuki</h1>

        <div class="prose max-w-none">
            {{ include('components/_pacte_content.html.twig') }}
        </div>

        <div class="text-center mt-12">
            <a href="{{ path('app_homepage') }}"
               class="inline-block bg-accent hover:bg-accent-dark text-white px-6 py-3 rounded-full shadow-lg text-lg transition">
                🏠 Retour à l'accueil
            </a>
        </div>
    </div>
{% endblock %}
