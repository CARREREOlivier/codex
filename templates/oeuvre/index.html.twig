{% extends 'base.html.twig' %}

{% block title %}Œuvres{% endblock %}

{% block body %}
    <div class="min-h-screen bg-parchment text-ink font-serif px-4 sm:px-6 lg:px-8 py-12">
        <div class="max-w-7xl mx-auto">
            <h1 class="text-5xl font-bold mb-2 text-center">Œuvres</h1>
            <br>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
                {% for oeuvre in oeuvres %}

                    <div class="bg-white border border-[#e0dcd3] rounded-xl shadow-md hover:shadow-lg transition duration-300 p-6">
                        <h2 class="text-2xl font-bold text-accent mb-2">{{ oeuvre.titre }}</h2>
                        <p class="text-gray-700 italic">{{ oeuvre.type }}</p>
                        <div class="text-gray-800">{{ oeuvre.description|raw }}</div>

                        <div class="flex justify-end space-x-4 mt-4">
                            <a href="{{ path('app_oeuvre_show', {'id': oeuvre.id}) }}"
                               class="text-blue-600 hover:text-blue-800" title="Voir">
                                📖
                            </a>
                            <a href="{{ path('app_oeuvre_edit', {'id': oeuvre.id}) }}"
                               class="text-yellow-600 hover:text-yellow-800" title="Modifier">
                                ✏️
                            </a>
                        </div>
                    </div>
                {% else %}
                    <p class="col-span-3 text-center text-gray-500">Aucune œuvre trouvée.</p>
                {% endfor %}
            </div>

            <div class="mt-8">
                <a href="{{ path('app_oeuvre_new') }}"
                   class="inline-block bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded shadow">
                    ➕ Créer une nouvelle œuvre
                </a>
            </div>
        </div>
    </div>
{% endblock %}
