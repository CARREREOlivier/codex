{% extends 'base.html.twig' %}

{% block title %}Edit Oeuvre{% endblock %}

{% block body %}
    <h1>Edit Oeuvre</h1>

    {{ include('oeuvres/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_oeuvre_index') }}">back to list</a>

    {{ include('oeuvres/_delete_form.html.twig') }}
{% endblock %}
